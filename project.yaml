version: '3.0'

expectations:
  population_size: 1000

actions:

  generate_study_population:
    run: cohortextractor:latest generate_cohort --study-definition study_definition_take1
    outputs:
      highly_sensitive:
        cohort: output/input.csv
        
  run_model:
    run: python:analysis/OS_EDA.do
    needs: [generate_study_population]
